# Version information for TRMNL Android app
# This is the single source of truth for version information
# Update this file when preparing a new release

# Version code (integer, must increase with each release)
# Used in: build.gradle.kts, metadata/ink.trmnl.android.yml
VERSION_CODE=15

# Version name (semantic versioning recommended)
# Used in: build.gradle.kts, metadata/ink.trmnl.android.yml
VERSION_NAME=1.9.4

# Git tag to use for this release (typically v + VERSION_NAME)
# Used in: metadata/ink.trmnl.android.yml
GIT_TAG=v1.9.4

# Short description of changes in this release
# Used for automatic changelog generation
RELEASE_NOTES=Support for custom server URLs for BYOS installations, improved refresh history logging, various UI improvements and bug fixes
